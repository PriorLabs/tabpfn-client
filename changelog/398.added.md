Opt-in `ClientOptions(with_download_uri=True)` has the server return predictions through a signed download URL, which the client fetches transparently; useful for large results such as regression `output_type="full"`.
