Now that hosted estimators are recognised as classifiers, scikit-learn's default cross-validation splitter for them is `StratifiedKFold` rather than `KFold`; cross-validation scores and anything derived from them may shift slightly.
