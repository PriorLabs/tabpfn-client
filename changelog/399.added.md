`ClientOptions(with_download_uri=...)` controls how predictions come back: `True` through a signed download URL, which the client fetches transparently; `False` inline, failing if the prediction does not fit; `None` (default) lets the server decide. The client accepts either response when the server decides.
