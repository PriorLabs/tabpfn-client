`ClientOptions(with_download_uri=True)` asks the server to return predictions through a signed download URL, which the client fetches transparently; useful for large results. The client also accepts such a response when the server chooses it on its own.
