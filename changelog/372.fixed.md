Hosted estimators are now recognised by scikit-learn as classifiers and regressors, so tools that dispatch on estimator type — partial dependence among them — accept them. Datasets holding missing or non-finite values can also be sent as JSON, which previously rejected them and left Parquet as the only option.
